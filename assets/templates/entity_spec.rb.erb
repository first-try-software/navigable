require_relative '../../../domain/<%= @resource_name %>/<%= @entity_name %>'

RSpec.describe <%= @entity_klass %> do
  subject(:entity) { <%= @entity_klass %>.new }

  describe '#valid?' do
    subject(:valid?) { entity.valid? }

    it { should be true }
  end
end
